---
phase: 15-navigation-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/docs.json
  - docs/agent-factory/*.mdx
autonomous: true
requirements:
  - FR-04
  - FR-05
user_setup: []
must_haves:
  truths:
    - docs.json Agent Factory group includes all 14 agent-factory pages in logical order
    - Mintlify build passes without navigation or link errors
    - All cross-links between Agent Factory pages resolve correctly
    - Citadel integration references are consistent across all Agent Factory pages
  artifacts:
    - path: docs/docs.json
      provides: Updated navigation with 14 Agent Factory pages in correct order
      contains: agent-factory/overview
    - path: docs/agent-factory/design-patterns.mdx
      provides: Design patterns with valid cross-links
    - path: docs/agent-factory/orchestration-patterns.mdx
      provides: Orchestration patterns with valid cross-links
  key_links:
    - from: docs/docs.json
      to: docs/agent-factory/design-patterns.mdx
      via: navigation entry
    - from: docs/agent-factory/design-patterns.mdx
      to: docs/agent-factory/orchestration-patterns.mdx
      via: Card href cross-link
---

<objective>
Validate and finalize docs/docs.json navigation for the 14 Agent Factory pages in the correct order. Ensure all cross-links and Citadel integration references across remaining Agent Factory pages are consistent and valid. The 4 use-case pages have been removed from the project.

Purpose: Complete the v1.2 milestone by ensuring the Agent Factory documentation section is fully navigable, internally consistent, and compliant with FR-04 and FR-05.
Output: Verified docs.json with complete Agent Factory navigation; fixed cross-links and Citadel references in affected MDX files.
</objective>

<execution_context>
@/home/khenzo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/khenzo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/docs.json
@docs/agent-factory/overview.mdx
@docs/agent-factory/citadel-mapping.mdx
@docs/agent-factory/design-patterns.mdx
@docs/agent-factory/agent-harness.mdx
@docs/agent-factory/context-engineering.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate docs.json Agent Factory navigation</name>
  <files>docs/docs.json</files>
  <action>
    Verify the "Agent Factory" navigation group in docs/docs.json includes exactly these 14 pages in the following logical order:
    1. overview
    2. intelligence-layers
    3. builder-personas
    4. citadel-mapping
    5. agentic-web-stack
    6. agentic-stack-diagrams
    7. design-patterns
    8. orchestration-patterns
    9. context-engineering
    10. agent-harness
    11. mcp-integration
    12. a2a-integration
    13. agent-security-blueprint
    14. agent-observability

    If any page is missing or out of order, correct it. Preserve all existing tabs, anchors, and other navigation groups exactly as they are. Ensure valid JSON formatting (no trailing commas).
  </action>
  <verify>
    - `cat docs/docs.json | jq '.navigation.groups[] | select(.group == "Agent Factory") | .pages'` shows all 14 pages in the correct order
    - `cat docs/docs.json | jq empty` returns no JSON syntax errors
  </verify>
  <done>docs.json contains all 14 Agent Factory pages in the specified order with valid JSON</done>
</task>

<task type="auto">
  <name>Task 2: Validate and fix cross-links across Agent Factory pages</name>
  <files>docs/agent-factory/*.mdx</files>
  <action>
    Scan all docs/agent-factory/*.mdx files for broken internal cross-links (href attributes in Card, Link, and markdown link syntax pointing to /agent-factory/* or /architecture/* paths). For each link found, verify the target file exists in the repository. Fix any broken links by updating them to the correct path. Pay special attention to:
    - Links from design-patterns to orchestration-patterns or other agent-factory pages
    - Links from overview.mdx to other agent-factory pages
    - Links to /architecture/citadel-4-layer-model and other architecture pages
    - Any stale references to removed use-case pages (use-case-employee-self-service, use-case-data-analytics, use-case-sales-engagement, use-case-customer-support)

    Do NOT change external URLs (GitHub, Microsoft docs, etc.).
  </action>
  <verify>
    Run a grep-based scan to list all /agent-factory/ and /architecture/ hrefs, then verify each target exists:
    - `grep -roh 'href="/agent-factory/[^"]*"' docs/agent-factory/*.mdx | sed 's|href="/agent-factory/||;s|"||' | while read p; do [ -f "docs/agent-factory/$p.mdx" ] || echo "MISSING: $p"; done`
    - `grep -roh 'href="/architecture/[^"]*"' docs/agent-factory/*.mdx | sed 's|href="/architecture/||;s|"||' | while read p; do [ -f "docs/architecture/$p.mdx" ] || echo "MISSING: $p"; done`
    Both loops should produce no MISSING output.
  </verify>
  <done>All internal cross-links in Agent Factory pages resolve to existing files</done>
</task>

<task type="auto">
  <name>Task 3: Validate Citadel integration references (FR-04)</name>
  <files>docs/agent-factory/*.mdx</files>
  <action>
    Verify that all 14 Agent Factory pages appropriately reference the Citadel 4-layer architecture (Layer 1 Governance Hub, Layer 2 AI Control Plane, Layer 3 Agent Identity, Layer 4 Security Fabric) and maintain cross-links to architecture/* pages. Specifically:

    - overview.mdx must have a "Citadel Governance Foundation" section with the 4-layer table
    - citadel-mapping.mdx must have the "Citadel 4-Layer + Agent Factory Mapping" table
    - agentic-web-stack.mdx must have "Citadel placement" for each of the 8 components
    - agent-harness.mdx must have a "Citadel Integration" section with a 4-layer mapping table
    - context-engineering.mdx must have a "Citadel Integration" section with a 4-layer mapping table
    - design-patterns.mdx and orchestration-patterns.mdx should reference Citadel layers in their pattern comparison tables or placement sections

    If any page is missing a Citadel placement section or has inconsistent naming (e.g., missing "Layer X" prefix, wrong layer numbers), add or fix it to match the established pattern.
  </action>
  <verify>
    - `grep -l "Layer 1\|Layer 2\|Layer 3\|Layer 4\|Citadel" docs/agent-factory/*.mdx` should match all 14 MDX files
    - `grep -c "## Citadel Integration" docs/agent-factory/agent-harness.mdx docs/agent-factory/context-engineering.mdx docs/agent-factory/citadel-mapping.mdx` should return at least 1 for each
    - `grep -c "Citadel Governance Foundation" docs/agent-factory/overview.mdx` should be 1
  </verify>
  <done>Every Agent Factory page contains appropriate Citadel 4-layer references with consistent structure</done>
</task>

<task type="auto">
  <name>Task 4: Run Mintlify build and verify success</name>
  <files>docs/docs.json</files>
  <action>
    Run `npm run build` from the project root to invoke the Mintlify build process. Capture the output. If the build fails due to navigation errors, broken links, or MDX syntax issues, fix the root cause and re-run until the build succeeds.
  </action>
  <verify>
    - `npm run build` exits with code 0
    - Build output does not contain errors related to docs.json navigation or missing pages
  </verify>
  <done>Mintlify build passes cleanly with no navigation or link errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds with exit code 0
- [ ] `jq` verification confirms all 14 Agent Factory pages are in docs.json in the correct order
- [ ] Cross-link scan confirms no MISSING internal links in Agent Factory pages
- [ ] Citadel reference scan confirms all 14 MDX files contain appropriate Citadel/Layer references
- [ ] No JSON syntax errors in docs.json
</verification>

<success_criteria>
- docs.json Agent Factory navigation includes exactly 14 pages in the correct order per FR-05
- All internal cross-links across Agent Factory pages resolve to existing files
- Citadel 4-layer integration references are present and consistent on every Agent Factory page (FR-04)
- Mintlify build passes without errors
- No unintended changes to other navigation groups, tabs, or anchors
</success_criteria>

<output>
After completion, create `.planning/phases/15-navigation-integration/15-01-SUMMARY.md`
</output>
