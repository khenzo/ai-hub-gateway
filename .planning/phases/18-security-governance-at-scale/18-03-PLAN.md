---
phase: 18-security-governance-at-scale
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
  - 18-02
files_modified:
  - docs/docs.json
autonomous: true
requirements:
  - GOVN-01
  - GOVN-02
  - GOVN-03

must_haves:
  truths:
    - "All three Phase 18 pages are reachable from the EY Alignment section sidebar navigation"
    - "The EY Alignment tab is already present in the nav (added in Phase 16) — Phase 18 extends the existing navigation group"
  artifacts:
    - path: "docs/docs.json"
      provides: "Updated navigation group for EY Alignment tab — adds governance-tiers, guardrails-config, compliance-tooling pages to the existing EY Alignment navigation group"
      contains: "ey-framework-alignment/governance-tiers"
  key_links:
    - from: "docs/docs.json navigation groups EY Framework Alignment"
      to: "docs/ey-framework-alignment/governance-tiers.mdx"
      via: "pages array entry"
      pattern: "ey-framework-alignment/governance-tiers"
    - from: "docs/docs.json navigation groups EY Framework Alignment"
      to: "docs/ey-framework-alignment/guardrails-config.mdx"
      via: "pages array entry"
      pattern: "ey-framework-alignment/guardrails-config"
    - from: "docs/docs.json navigation groups EY Framework Alignment"
      to: "docs/ey-framework-alignment/compliance-tooling.mdx"
      via: "pages array entry"
      pattern: "ey-framework-alignment/compliance-tooling"
---

<objective>
Wire the three Phase 18 pages into the existing EY Alignment navigation group in docs.json.

Purpose: Pages created in Plans 01 and 02 are invisible in the Mintlify site until listed in docs.json. Phase 16 already added the "EY Alignment" tab and a navigation group. This plan extends that group with the three new pages under a "Security & Governance" sub-group.

Output: Updated `docs/docs.json` with three new page entries in the EY Alignment navigation.
</objective>

<execution_context>
@/home/khenzo/.claude/get-shit-done/workflows/execute-plan.md
@/home/khenzo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/docs.json
</context>

<interfaces>
<!-- Current EY Alignment navigation group (added by Phase 16-02-PLAN): -->
<!--
{
  "group": "EY Framework Alignment",
  "tab": "EY Alignment",
  "pages": [
    "ey-framework-alignment/index",
    "ey-framework-alignment/alignment-map"
  ]
}

Phase 18 must EXTEND this group by adding a nested sub-group for Security & Governance.
Do NOT replace or remove the existing two pages.
-->

<!-- Mintlify nested group pattern (from prior phases): -->
<!--
  Nested groups are objects within the pages array:
  {
    "group": "Security & Governance",
    "icon": "shield",
    "pages": [
      "ey-framework-alignment/governance-tiers",
      "ey-framework-alignment/guardrails-config",
      "ey-framework-alignment/compliance-tooling"
    ]
  }
-->

<!-- Three pages to add (created in Plans 01 and 02): -->
<!--
  ey-framework-alignment/governance-tiers    (Plan 01 Task 1)
  ey-framework-alignment/guardrails-config   (Plan 02 Task 1)
  ey-framework-alignment/compliance-tooling  (Plan 01 Task 2)
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend EY Alignment navigation group with Security & Governance sub-group</name>
  <files>docs/docs.json</files>
  <action>
Read `docs/docs.json` fully. Locate the navigation group with `"group": "EY Framework Alignment"` and `"tab": "EY Alignment"`.

**Current state of that group:**
```json
{
  "group": "EY Framework Alignment",
  "tab": "EY Alignment",
  "pages": [
    "ey-framework-alignment/index",
    "ey-framework-alignment/alignment-map"
  ]
}
```

**Required change:** Append a nested sub-group object to the `pages` array of that group. The result must look like this:
```json
{
  "group": "EY Framework Alignment",
  "tab": "EY Alignment",
  "pages": [
    "ey-framework-alignment/index",
    "ey-framework-alignment/alignment-map",
    {
      "group": "Security & Governance",
      "icon": "shield",
      "pages": [
        "ey-framework-alignment/governance-tiers",
        "ey-framework-alignment/guardrails-config",
        "ey-framework-alignment/compliance-tooling"
      ]
    }
  ]
}
```

**Rules:**
- Do NOT alter any other navigation group, tab, or page entry in docs.json
- Preserve all existing formatting (2-space indentation, trailing commas where present)
- The file must remain valid JSON after the edit
- The "EY Framework Alignment" group's existing two pages (`index` and `alignment-map`) must remain in the pages array

**Verification mental check:** After the edit, the EY Framework Alignment group pages array has 3 items: two strings and one nested group object. The total docs.json tab count remains at 7 (or whatever Phase 16 set it to — do not add or remove tabs).
  </action>
  <verify>
    <automated>python3 -c "
import json
path = '/home/khenzo/projects/ai-hub-gateway/docs/docs.json'
d = json.load(open(path))
groups = d['navigation']['groups']
ey_group = next((g for g in groups if g.get('group') == 'EY Framework Alignment'), None)
assert ey_group is not None, 'EY Framework Alignment group missing'
pages = ey_group['pages']
assert 'ey-framework-alignment/index' in pages, 'index page missing'
assert 'ey-framework-alignment/alignment-map' in pages, 'alignment-map page missing'
nested = next((p for p in pages if isinstance(p, dict) and p.get('group') == 'Security & Governance'), None)
assert nested is not None, 'Security & Governance sub-group missing'
sub_pages = nested['pages']
assert 'ey-framework-alignment/governance-tiers' in sub_pages, 'governance-tiers missing'
assert 'ey-framework-alignment/guardrails-config' in sub_pages, 'guardrails-config missing'
assert 'ey-framework-alignment/compliance-tooling' in sub_pages, 'compliance-tooling missing'
print('VALID')
"</automated>
  </verify>
  <done>docs.json is valid JSON; EY Framework Alignment group still contains index and alignment-map; new Security & Governance sub-group present with all three Phase 18 pages; no other navigation entries modified</done>
</task>

</tasks>

<verification>
- `python3 -c "import json; json.load(open('docs/docs.json'))"` exits cleanly
- EY Framework Alignment group pages: index, alignment-map, + Security & Governance sub-group
- Sub-group pages: governance-tiers, guardrails-config, compliance-tooling
- No other groups, tabs, or pages in docs.json changed
- Total Phase 18 pages are now navigable from the EY Alignment tab sidebar
</verification>

<success_criteria>
- All three Phase 18 content pages are reachable from the EY Alignment tab sidebar navigation
- docs.json remains valid JSON with no regressions to existing navigation
- Phase 16 pages (index, alignment-map) remain intact
</success_criteria>

<output>
After completion, create `.planning/phases/18-security-governance-at-scale/18-03-SUMMARY.md`
</output>
