---
phase: 11-open-standards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/agent-factory/mcp-integration.mdx
  - docs.json
autonomous: true
requirements:
  - 11-01

must_haves:
  truths:
    - User can read detailed MCP integration documentation
    - MCP page appears in the Agent Factory navigation group
    - Document includes MCP architecture on Citadel, gateway patterns, tool invocation flows, and security guidance
    - Navigation links connect MCP content to related Agent Factory pages
  artifacts:
    - path: "docs/agent-factory/mcp-integration.mdx"
      provides: "MCP server integration and tool invocation patterns on Citadel"
      min_lines: 80
    - path: "docs.json"
      provides: "Navigation includes MCP integration page"
      contains: "mcp-integration"
  key_links:
    - from: "docs.json"
      to: "docs/agent-factory/mcp-integration.mdx"
      via: "Mintlify navigation"
---

<objective>
Document MCP (Model Context Protocol) server integration and tool invocation patterns within the AI Landing Zone and Citadel architecture.

Purpose: Provide architects with clear guidance on how to expose, govern, and secure enterprise tools via MCP through the Citadel Governance Hub, enabling agents to invoke APIs and functions at scale.
Output: `docs/agent-factory/mcp-integration.mdx` and updated `docs.json` navigation.
</objective>

<execution_context>
@/home/khenzo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/khenzo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Existing References
@docs/agent-factory/agentic-web-stack.mdx — Communication and Tools components, mentions MCP servers
@docs/agent-factory/design-patterns.mdx — Tool Use pattern with APIM as MCP gateway guidance
@docs/agent-factory/citadel-mapping.mdx — Foundry IQ and Layer 1 gateway for MCP traffic
@docs/agent-factory/orchestration-patterns.mdx — Layer 1 secures inter-agent communication via MCP
@docs/architecture/layer-1-governance-hub.mdx — AI Registry catalog of MCP servers
@docs/guides/reference/governance-benefits.mdx — AI registry and MCP tools mentions
@docs.json — Navigation structure to update
</context>

<tasks>

<task type="auto">
  <name>Create MCP integration documentation</name>
  <files>docs/agent-factory/mcp-integration.mdx</files>
  <action>
    Create `docs/agent-factory/mcp-integration.mdx` following established Mintlify documentation conventions:

    1. Frontmatter with title "MCP Integration" and description about Model Context Protocol on Citadel
    2. H1 heading and overview paragraph explaining MCP as an open standard for structured tool and context exchange
    3. "What is MCP?" section describing the Model Context Protocol, its origin (Anthropic), and why it matters for enterprise agent systems
    4. "MCP Architecture on Citadel" section:
       - Layer 1 (Governance Hub / APIM) as the unified MCP gateway
       - Layer 2 (AI Control Plane) for tool usage telemetry and evaluations
       - Layer 3 (Agent Identity) for authenticating which agents can invoke which tools
       - Layer 4 (Security Fabric) for content filtering and data governance on tool inputs/outputs
       - Include a simple ASCII diagram showing: Agent -> APIM -> MCP Server -> Enterprise API
    5. "Gateway Patterns" section:
       - APIM as the central MCP gateway (authentication, rate limiting, policy enforcement)
       - API Center for MCP server catalog and versioning
       - Routing MCP requests through the CGH to maintain auditability
    6. "Tool Invocation Patterns" section:
       - Direct tool invocation via Foundry Agent Service tools
       - Dynamic tool discovery from the AI Registry / API Center
       - Batched and async tool calls for high-throughput scenarios
       - Error handling and retry patterns
    7. "Security & Governance" section:
       - Entra ID authentication on every MCP call
       - Rate limiting and quota management at the gateway
       - Input/output content safety filtering
       - Audit logging of all tool invocations
    8. "Implementation Guidance" section:
       - Register MCP servers in API Center
       - Expose through APIM with consistent auth policies
       - Use the AI Registry to catalog available tools for agents
       - Monitor tool usage via Foundry Control Plane telemetry
    9. "Next Steps" CardGroup linking to a2a-integration, agentic-web-stack, and design-patterns pages

    Use consistent Mintlify MDX syntax. Match the tone and formatting of design-patterns.mdx and agentic-web-stack.mdx.
  </action>
  <verify>File exists with valid MDX frontmatter, at least 80 lines, and contains key sections (What is MCP, Architecture, Gateway Patterns, Tool Invocation, Security, Implementation Guidance)</verify>
  <done>MCP integration document created</done>
</task>

<task type="auto">
  <name>Update docs.json navigation</name>
  <files>docs.json</files>
  <action>
    Edit docs.json to add `docs/agent-factory/mcp-integration` to the Agent Factory navigation group.

    Insert after `docs/agent-factory/orchestration-patterns` and before `docs/agent-factory/use-case-employee-self-service`:
    - `docs/agent-factory/mcp-integration`

    Maintain proper JSON structure and trailing commas. Validate JSON after editing.
  </action>
  <verify>
    - docs.json is valid JSON
    - Agent Factory group contains `mcp-integration` in the correct order
  </verify>
  <done>Navigation updated with MCP integration page</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docs/agent-factory/mcp-integration.mdx` exists with valid frontmatter and >= 80 lines
- [ ] Document includes What is MCP, Architecture on Citadel, Gateway Patterns, Tool Invocation, Security & Governance, and Implementation Guidance sections
- [ ] docs.json is valid JSON and includes `mcp-integration` in the Agent Factory group
- [ ] CardGroup next-step navigation links to valid existing pages
</verification>

<success_criteria>
- All tasks completed
- MCP integration document created with consistent structure and Mintlify MDX syntax
- Navigation updated and valid
- Content accurately represents MCP as an open standard integrated with Citadel governance
- Citadel layer mapping and security guidance included
</success_criteria>

<output>
After completion, create `.planning/phases/11-open-standards/11-01-SUMMARY.md`
</output>
