---
phase: 11-open-standards
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - docs/agent-factory/a2a-integration.mdx
  - docs.json
autonomous: true
requirements:
  - 11-02

must_haves:
  truths:
    - User can read detailed A2A integration documentation
    - A2A page appears in the Agent Factory navigation group
    - Document includes A2A architecture on Citadel, cross-agent collaboration patterns, gateway patterns, and security guidance
    - Navigation links connect A2A content to related Agent Factory pages
  artifacts:
    - path: "docs/agent-factory/a2a-integration.mdx"
      provides: "A2A cross-agent collaboration and interoperability on Citadel"
      min_lines: 80
    - path: "docs.json"
      provides: "Navigation includes A2A integration page"
      contains: "a2a-integration"
  key_links:
    - from: "docs.json"
      to: "docs/agent-factory/a2a-integration.mdx"
      via: "Mintlify navigation"
---

<objective>
Document A2A (Agent-to-Agent) cross-agent collaboration and interoperability patterns within the AI Landing Zone and Citadel architecture.

Purpose: Provide architects with clear guidance on how to enable secure, governable agent-to-agent communication via A2A through the Citadel Governance Hub, supporting multi-agent workflows at scale.
Output: `docs/agent-factory/a2a-integration.mdx` and updated `docs.json` navigation.
</objective>

<execution_context>
@/home/khenzo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/khenzo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Existing References
@docs/agent-factory/agentic-web-stack.mdx — Communication component, mentions A2A protocol
@docs/agent-factory/orchestration-patterns.mdx — Multi-agent patterns, Layer 1 secures A2A communication
@docs/agent-factory/design-patterns.mdx — Planning and tool-use patterns that combine into multi-agent workflows
@docs/agent-factory/citadel-mapping.mdx — Citadel layer mappings and governance patterns
@docs.json — Navigation structure to update
</context>

<tasks>

<task type="auto">
  <name>Create A2A integration documentation</name>
  <files>docs/agent-factory/a2a-integration.mdx</files>
  <action>
    Create `docs/agent-factory/a2a-integration.mdx` following established Mintlify documentation conventions:

    1. Frontmatter with title "A2A Integration" and description about Agent-to-Agent protocol on Citadel
    2. H1 heading and overview paragraph explaining A2A as an open standard for cross-agent collaboration and interoperability
    3. "What is A2A?" section describing the Agent-to-Agent protocol, its purpose (Google's A2A initiative / open standard), and why it matters for enterprise multi-agent systems
    4. "A2A Architecture on Citadel" section:
       - Layer 1 (Governance Hub / APIM) as the A2A message gateway
       - Layer 2 (AI Control Plane) for execution traces and evaluations of agent collaborations
       - Layer 3 (Agent Identity) for authenticating agents and enforcing which agents can communicate
       - Layer 4 (Security Fabric) for monitoring and threat detection on inter-agent traffic
       - Include a simple ASCII diagram showing: Agent A -> APIM -> Agent B, with identity and policy enforcement at the gateway
    5. "Cross-Agent Collaboration Patterns" section:
       - Delegation pattern: one agent delegates a subtask to another specialist agent
       - Coordination pattern: multiple agents coordinate on a shared goal via planning
       - Handoff pattern: transferring a session from one agent to another with context preservation
       - Broadcast pattern: one agent publishes an event that multiple agents consume
    6. "Gateway Patterns" section:
       - APIM as the central A2A gateway (message routing, authentication, rate limiting)
       - Message validation and schema enforcement at the edge
       - Audit logging of all inter-agent communication
       - Integration with Agent 365 for identity-verified messaging
    7. "Security & Identity" section:
       - Mutual authentication between agents via Entra ID / Agent 365
       - Authorization policies defining allowed agent-to-agent communication paths
       - Message encryption and replay protection
       - Monitoring for anomalous agent behavior
    8. "Implementation Guidance" section:
       - Define agent capability contracts (what each agent can do)
       - Route all A2A traffic through APIM for consistent governance
       - Use Foundry Control Plane to trace multi-agent workflows
       - Establish clear escalation paths when agent handoffs fail
    9. "Next Steps" CardGroup linking to mcp-integration, orchestration-patterns, and agentic-web-stack pages

    Use consistent Mintlify MDX syntax. Match the tone and formatting of design-patterns.mdx and agentic-web-stack.mdx.
  </action>
  <verify>File exists with valid MDX frontmatter, at least 80 lines, and contains key sections (What is A2A, Architecture, Collaboration Patterns, Gateway Patterns, Security, Implementation Guidance)</verify>
  <done>A2A integration document created</done>
</task>

<task type="auto">
  <name>Update docs.json navigation</name>
  <files>docs.json</files>
  <action>
    Edit docs.json to add `docs/agent-factory/a2a-integration` to the Agent Factory navigation group.

    Insert after `docs/agent-factory/mcp-integration` and before `docs/agent-factory/use-case-employee-self-service`:
    - `docs/agent-factory/a2a-integration`

    Maintain proper JSON structure and trailing commas. Validate JSON after editing.
  </action>
  <verify>
    - docs.json is valid JSON
    - Agent Factory group contains `a2a-integration` in the correct order
  </verify>
  <done>Navigation updated with A2A integration page</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docs/agent-factory/a2a-integration.mdx` exists with valid frontmatter and >= 80 lines
- [ ] Document includes What is A2A, Architecture on Citadel, Cross-Agent Collaboration Patterns, Gateway Patterns, Security & Identity, and Implementation Guidance sections
- [ ] docs.json is valid JSON and includes `a2a-integration` in the Agent Factory group
- [ ] CardGroup next-step navigation links to valid existing pages
</verification>

<success_criteria>
- All tasks completed
- A2A integration document created with consistent structure and Mintlify MDX syntax
- Navigation updated and valid
- Content accurately represents A2A as an open standard integrated with Citadel governance
- Citadel layer mapping and security guidance included
</success_criteria>

<output>
After completion, create `.planning/phases/11-open-standards/11-02-SUMMARY.md`
</output>
