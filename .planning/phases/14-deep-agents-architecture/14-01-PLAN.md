---
phase: 14-deep-agents-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/agent-factory/agent-harness.mdx
  - docs/agent-factory/context-engineering.mdx
autonomous: true
requirements:
  - FR-03

must_haves:
  truths:
    - "agent-harness.mdx exists with all FR-03 required sections: tool-gated execution, middleware chain, HITL hooks, 4-layer security model, sandbox providers, Citadel integration table"
    - "context-engineering.mdx exists with all FR-03 required sections: filesystem working memory, progressive skill disclosure, 3-tier loading, automatic summarization, skills vs memory distinction"
    - "context-engineering.mdx has a formal ## Citadel Integration section with a 4-layer mapping table (not just inline Citadel placement paragraphs)"
    - "docs.json has context-engineering at position 9 and agent-harness at position 10 in the Agent Factory group, with no duplicate entries"
    - "All CardGroup next-step links in both pages point to pages that exist in navigation"
  artifacts:
    - path: "docs/agent-factory/agent-harness.mdx"
      provides: "Harness engineering page covering tool-gated execution, middleware, HITL, sandboxes, Citadel mapping"
      min_lines: 140
    - path: "docs/agent-factory/context-engineering.mdx"
      provides: "Context engineering page covering working memory, skill disclosure, summarization, skills vs memory"
      min_lines: 175
    - path: "docs/docs.json"
      provides: "Navigation with both pages wired in correct order"
      contains: "agent-factory/context-engineering"
  key_links:
    - from: "docs/agent-factory/context-engineering.mdx"
      to: "## Citadel Integration"
      via: "formal H2 section with 4-layer mapping table"
      pattern: "## Citadel Integration"
    - from: "docs/agent-factory/agent-harness.mdx"
      to: "docs/agent-factory/context-engineering.mdx"
      via: "CardGroup Card href"
      pattern: "agent-factory/context-engineering"
    - from: "docs/agent-factory/context-engineering.mdx"
      to: "docs/agent-factory/agent-harness.mdx"
      via: "CardGroup Card href"
      pattern: "agent-factory/agent-harness"
---

<objective>
Verify both Phase 14 deliverable pages (agent-harness.mdx and context-engineering.mdx) satisfy FR-03 line by line, close the one confirmed gap — a missing formal Citadel Integration section in context-engineering.mdx — and confirm docs.json navigation is correctly ordered with no duplicate entries.

Purpose: Both files were shipped in a prior session but context-engineering.mdx uses inline Citadel placement paragraphs rather than the formal ## Citadel Integration section with 4-layer table that is the project standard (see agent-harness.mdx, agent-security-blueprint.mdx, design-patterns.mdx). This gap must be closed before Phase 15 (Navigation & Integration) begins.

Output: Both pages fully compliant with FR-03 and FR-04. docs.json wiring confirmed. Phase 14 complete.
</objective>

<execution_context>
@/home/khenzo/.claude/get-shit-done/workflows/execute-plan.md
@/home/khenzo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-deep-agents-architecture/14-RESEARCH.md

<interfaces>
<!-- Key patterns the executor must follow — extracted from established project pages. -->

From docs/agent-factory/agent-harness.mdx — Citadel Integration table pattern:
```markdown
## Citadel Integration

The harness architecture maps directly onto the Citadel 4-layer model:

| Harness Layer | Citadel Layer | Integration |
|---------------|---------------|-------------|
| Tool gateway (all tool calls) | **Layer 1 (Governance Hub)** | APIM enforces auth, rate limiting, and policy on every tool invocation |
| HITL middleware & audit trails | **Layer 2 (AI Control Plane)** | Execution traces capture every tool call for observability and compliance |
| Agent identity for tool access | **Layer 3 (Agent Identity)** | Tool access scoped to agent's registered permissions and access packages |
| Sandbox isolation & PII filtering | **Layer 4 (Security Fabric)** | Content safety applied to tool outputs; threat detection monitors agent behavior |
```

From docs/agent-factory/context-engineering.mdx — current (incomplete) Citadel references:
- Line ~72: inline `**Citadel placement:** Working memory storage is governed by **Layer 4 (Security Fabric)**...`
- Line ~158: inline `**Citadel placement:** Context engineering operates within **Layer 2 (AI Control Plane)**...`

These two inline paragraphs must be consolidated into a formal ## Citadel Integration section with a 4-layer table added BEFORE the ## Best Practices section (line ~160). The inline paragraphs may be retained in their local sections or replaced — keep whichever reads best.

Context-engineering Citadel mapping (from RESEARCH.md):
| Context Engineering Concept | Citadel Layer | Integration |
|-----------------------------|---------------|-------------|
| Working memory storage | **Layer 4 (Security Fabric)** | Data governance policies govern agent working memory; retention and cleanup enforced via APIM |
| Execution traces, skill loading | **Layer 2 (AI Control Plane)** | Observability captures context decisions — skills loaded, memory access patterns, summarization events |
| Agent identity for memory access | **Layer 3 (Agent Identity)** | Memory access scoped to agent's registered permissions; prevents cross-agent data leakage |
| Skill discovery and policy | **Layer 1 (Governance Hub)** | APIM governs which skill repositories agents can query; policy controls skill availability by environment |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify agent-harness.mdx against FR-03 checklist</name>
  <files>docs/agent-factory/agent-harness.mdx</files>
  <action>
Read docs/agent-factory/agent-harness.mdx in full. Verify every FR-03 checklist item from RESEARCH.md is present:

- Harness engineering concept defined
- Tool-gated execution: "model never executes anything directly" principle
- Middleware chain: path validation → HITL → execution isolation → sandbox
- HITL hooks: wrap_tool_call, per-session allow-lists, auto-approval learning
- 4-layer security model with ASCII diagram
- Sandbox providers: Modal, Daytona, Runloop with comparison table
- Sandbox as Tool pattern (recommended) vs Agent in Sandbox (risky)
- Citadel integration section mapping all 4 layers with formal table
- Network egress controls
- Next Steps CardGroup with valid links (context-engineering, agent-security-blueprint, mcp-integration, citadel-mapping)

If all items are present and substantive: no file modification needed. Document the verification result.

If any item is missing or only stub-level: add the missing content using the project MDX conventions — ASCII art for diagrams, markdown pipe tables for comparisons, Warning/Note Mintlify components for security callouts. Do NOT rewrite working sections. Do NOT overwrite the file if no gaps are found.
  </action>
  <verify>
    <automated>grep -c "Citadel\|Layer [1-4]\|sandbox\|HITL\|middleware" /home/khenzo/projects/ai-hub-gateway/docs/agent-factory/agent-harness.mdx</automated>
  </verify>
  <done>agent-harness.mdx verified complete against all FR-03 checklist items. Line count >= 140. Citadel Integration table present with all 4 layers mapped. CardGroup links valid.</done>
</task>

<task type="auto">
  <name>Task 2: Add formal Citadel Integration section to context-engineering.mdx</name>
  <files>docs/agent-factory/context-engineering.mdx</files>
  <action>
Read docs/agent-factory/context-engineering.mdx in full. Verify the FR-03 checklist:

- Context engineering discipline defined
- Three-tier memory hierarchy (Immediate Context / Working Memory / Archived History + Skills)
- Filesystem-backed working memory: deliberate offloading, automatic summarization, result eviction
- Progressive skill disclosure: SKILL.md structure with YAML frontmatter
- Three-tier loading diagram and explanation
- Skills vs Memory distinction (table)
- Automatic context compression
- Context Engineering vs Harness Engineering relationship
- Citadel integration section (FORMAL — see below)
- Next Steps CardGroup with valid links

The confirmed gap: context-engineering.mdx has two inline `**Citadel placement:**` paragraphs but NO formal `## Citadel Integration` section with a 4-layer mapping table. This is required by FR-04 and the project pattern.

Add a `## Citadel Integration` section BEFORE `## Best Practices`. Use this content (adapting prose as needed):

```markdown
## Citadel Integration

Context engineering integrates with all four Citadel governance layers:

| Context Engineering Layer | Citadel Layer | Integration |
|---------------------------|---------------|-------------|
| Working memory storage | **Layer 4 (Security Fabric)** | Data governance policies govern agent working memory; retention and cleanup enforced at the storage layer |
| Execution traces & skill loading | **Layer 2 (AI Control Plane)** | Observability captures context decisions — skills loaded, memory access patterns, summarization events |
| Agent identity for memory access | **Layer 3 (Agent Identity)** | Memory access scoped to agent's registered permissions; prevents cross-agent data leakage |
| Skill discovery and policy | **Layer 1 (Governance Hub)** | APIM governs which skill repositories agents can query; policy controls skill availability by environment |
```

Keep the existing inline `**Citadel placement:**` paragraphs in their local sections — they provide useful in-context annotation and should not be removed. The formal table in the new section provides the consolidated view.

Do NOT modify any other sections. Do NOT change frontmatter, headings, or CardGroup links.
  </action>
  <verify>
    <automated>grep -c "## Citadel Integration\|Layer [1-4]" /home/khenzo/projects/ai-hub-gateway/docs/agent-factory/context-engineering.mdx</automated>
  </verify>
  <done>context-engineering.mdx has a formal ## Citadel Integration section with a 4-column table mapping all 4 Citadel layers. grep output >= 5. File line count >= 195. All other FR-03 items verified present.</done>
</task>

<task type="auto">
  <name>Task 3: Verify docs.json navigation ordering and cross-links</name>
  <files>docs/docs.json</files>
  <action>
Verify docs.json navigation for the Agent Factory group:

1. Confirm context-engineering appears at position 9 in the Agent Factory navigation group (after orchestration-patterns, before agent-harness).
2. Confirm agent-harness appears at position 10 (after context-engineering, before mcp-integration).
3. Confirm no duplicate entries for either page.
4. Confirm the full FR-05 ordering: overview → intelligence-layers → builder-personas → citadel-mapping → agentic-web-stack → agentic-stack-diagrams → design-patterns → orchestration-patterns → context-engineering → agent-harness → mcp-integration → a2a-integration → agent-security-blueprint → agent-observability.

If the ordering is correct and no duplicates exist: no file modification needed. Document verification result.

If ordering is wrong or entries are missing/duplicated: fix by editing only the Agent Factory navigation group in docs.json. Preserve all other navigation groups unchanged.

Also verify cross-links between pages:
- agent-harness.mdx CardGroup includes href="/agent-factory/context-engineering"
- context-engineering.mdx CardGroup includes href="/agent-factory/agent-harness"
- Both pages link to agent-security-blueprint, which exists at docs/agent-factory/agent-security-blueprint.mdx
- context-engineering.mdx links to agent-observability, which exists at docs/agent-factory/agent-observability.mdx
  </action>
  <verify>
    <automated>node -e "const d=require('/home/khenzo/projects/ai-hub-gateway/docs/docs.json'); const af=d.navigation.find(g=>g.group==='Agent Factory')||d.tabs?.flatMap(t=>t.groups||[]).find(g=>g.group==='Agent Factory'); const pages=af?.pages||[]; const ci=pages.indexOf('agent-factory/context-engineering'); const ah=pages.indexOf('agent-factory/agent-harness'); console.log('context-engineering position:', ci, '| agent-harness position:', ah, '| gap:', ah-ci);"</automated>
  </verify>
  <done>docs.json verified: context-engineering at position 9, agent-harness at position 10, no duplicates. Cross-links between pages confirmed valid. No file modification required if already correct.</done>
</task>

</tasks>

<verification>
Run the VALIDATION.md full suite command to confirm all files are substantive:

```bash
node -e "require('fs'); ['docs/agent-factory/agent-harness.mdx','docs/agent-factory/context-engineering.mdx'].forEach(f => { const c = require('fs').readFileSync(f,'utf8'); console.log(f, c.length > 1000 ? 'OK (' + c.length + ' chars)' : 'TOO SHORT'); })"
```

Run Citadel section check:
```bash
grep -c "## Citadel Integration" docs/agent-factory/agent-harness.mdx docs/agent-factory/context-engineering.mdx
```

Both must return 1 (each file has exactly one Citadel Integration section).
</verification>

<success_criteria>
- agent-harness.mdx: All FR-03 harness checklist items present, formal Citadel Integration table maps all 4 layers, line count >= 140
- context-engineering.mdx: All FR-03 context checklist items present, formal ## Citadel Integration section added with 4-layer table, line count >= 195
- docs.json: context-engineering at position 9, agent-harness at position 10, no duplicates, FR-05 ordering satisfied
- All cross-links in both pages point to pages that exist in docs.json navigation
- Phase 14 declared complete — both pages fully compliant with FR-03 and FR-04
</success_criteria>

<output>
After completion, create `.planning/phases/14-deep-agents-architecture/14-01-SUMMARY.md` with:
- What was verified (FR-03 checklist results for each file)
- What gap was found and closed (context-engineering.mdx Citadel Integration section)
- docs.json verification result
- Final line counts for both files
- Phase 14 completion declaration
</output>
