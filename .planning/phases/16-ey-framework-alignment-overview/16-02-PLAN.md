---
phase: 16-ey-framework-alignment-overview
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - docs/docs.json
autonomous: true
requirements:
  - ALIGN-02

must_haves:
  truths:
    - "Reader can see an EY Framework Alignment tab in the Mintlify top-level navigation bar without needing to search"
    - "Clicking the EY Framework Alignment tab shows the framework overview page (index.mdx)"
    - "The alignment map page is reachable from the section navigation sidebar"
  artifacts:
    - path: "docs/docs.json"
      provides: "Updated Mintlify navigation config with 7th tab and navigation group for EY section"
      contains: "ey-framework-alignment"
  key_links:
    - from: "docs/docs.json tabs array"
      to: "docs/ey-framework-alignment/index.mdx"
      via: "tab url: ey-framework-alignment"
      pattern: "\"url\": \"ey-framework-alignment\""
    - from: "docs/docs.json navigation groups"
      to: "docs/ey-framework-alignment/alignment-map.mdx"
      via: "pages array entry: ey-framework-alignment/alignment-map"
      pattern: "ey-framework-alignment/alignment-map"
---

<objective>
Wire the two EY alignment pages created in Plan 01 into the Mintlify docs.json navigation so the section appears as a top-level tab and both pages are reachable from the sidebar.

Purpose: ALIGN-02 requires the EY section to be navigable from the Mintlify top-level navigation without searching. Mintlify derives its tab bar and sidebar entirely from docs.json — no page is visible without a navigation entry.

Output: Updated `docs/docs.json` with a 7th tab "EY Alignment" and a matching navigation group containing both pages.
</objective>

<execution_context>
@/home/khenzo/.claude/get-shit-done/workflows/execute-plan.md
@/home/khenzo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/docs.json
</context>

<interfaces>
<!-- Current docs.json tabs array (6 existing tabs): -->
```json
"tabs": [
  { "name": "Getting Started", "url": "getting-started" },
  { "name": "Architecture",    "url": "architecture"    },
  { "name": "Agent Factory",   "url": "agent-factory"   },
  { "name": "Guides",          "url": "guides"          },
  { "name": "Governance",      "url": "governance"      },
  { "name": "Operations",      "url": "operations"      }
]
```

<!-- Current navigation.groups array (each group binds to a tab via "tab" field): -->
Each group object has: { "group": string, "tab": string, "pages": string[] }
Some groups have nested sub-groups (pages containing objects with "group"/"icon"/"pages").

<!-- Pages created in Plan 01 (must be referenced in navigation): -->
- ey-framework-alignment/index        (overview / landing page)
- ey-framework-alignment/alignment-map (full criterion table)

<!-- Mintlify tab-binding convention (from existing patterns): -->
The "tab" field in a navigation group MUST match the "name" field of a tab in the tabs array exactly.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add EY Alignment tab and navigation group to docs.json</name>
  <files>docs/docs.json</files>
  <action>
Read `docs/docs.json` fully. Make two targeted edits:

**Edit 1 — Add 7th tab to the `tabs` array:**
Append the following object after the "Operations" tab entry:
```json
{
  "name": "EY Alignment",
  "url": "ey-framework-alignment"
}
```

**Edit 2 — Add navigation group to the `navigation.groups` array:**
Append the following group object at the end of the `navigation.groups` array (after the "Operations" groups):
```json
{
  "group": "EY Framework Alignment",
  "tab": "EY Alignment",
  "pages": [
    "ey-framework-alignment/index",
    "ey-framework-alignment/alignment-map"
  ]
}
```

The "tab" value "EY Alignment" MUST match exactly the "name" field added in Edit 1.

IMPORTANT: Do not alter any other part of docs.json. Preserve all existing formatting, indentation (2-space), and trailing comma conventions. The file must remain valid JSON after the edit.

Verify JSON validity mentally: the tabs array gains one entry; the navigation.groups array gains one entry. Both arrays were already comma-separated — ensure the new items are properly comma-separated from prior items.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('/home/khenzo/projects/ai-hub-gateway/docs/docs.json')); tabs=[t['name'] for t in d['tabs']]; groups=[g['group'] for g in d['navigation']['groups']]; assert 'EY Alignment' in tabs, 'Tab missing'; assert 'EY Framework Alignment' in groups, 'Group missing'; pages=next(g['pages'] for g in d['navigation']['groups'] if g['group']=='EY Framework Alignment'); assert 'ey-framework-alignment/index' in pages; assert 'ey-framework-alignment/alignment-map' in pages; print('VALID')"</automated>
  </verify>
  <done>docs.json is valid JSON; tabs array contains "EY Alignment" tab; navigation.groups contains "EY Framework Alignment" group with both pages listed; no existing navigation entries removed or altered</done>
</task>

</tasks>

<verification>
- `python3 -c "import json; json.load(open('docs/docs.json'))"` exits with no error (valid JSON)
- Tab "EY Alignment" present in tabs array with url "ey-framework-alignment"
- Navigation group "EY Framework Alignment" present with tab binding "EY Alignment"
- Group pages include both ey-framework-alignment/index and ey-framework-alignment/alignment-map
- Total tabs count is now 7 (was 6)
- All prior 6 tabs and their navigation groups remain intact and unchanged
</verification>

<success_criteria>
- docs.json updated with 7th tab "EY Alignment" and matching navigation group
- Both EY section pages (index, alignment-map) appear in the navigation group pages list
- docs.json remains valid JSON with no regressions to existing navigation entries
- Reader can navigate to EY Framework Alignment tab from top-level Mintlify nav (ALIGN-02 satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/16-ey-framework-alignment-overview/16-02-SUMMARY.md`
</output>
