---
phase: 20-attention-points-recommendations
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - docs/docs.json
autonomous: true
requirements:
  - GAPS-01

must_haves:
  truths:
    - "Reader can navigate to the Attention Points \u0026 Recommendations page from the EY Alignment section sidebar"
    - "The EY Alignment navigation group in docs.json includes the gaps-and-recommendations page as the final item"
    - "docs.json remains valid JSON and Mintlify build succeeds"
  artifacts:
    - path: "docs/docs.json"
      provides: "Updated Mintlify navigation config with gaps-and-recommendations added to EY Alignment group"
      contains: "ey-framework-alignment/gaps-and-recommendations"
  key_links:
    - from: "docs/docs.json navigation.groups EY Alignment section"
      to: "docs/ey-framework-alignment/gaps-and-recommendations.mdx"
      via: "pages array entry: ey-framework-alignment/gaps-and-recommendations"
      pattern: "ey-framework-alignment/gaps-and-recommendations"
---

<objective>
Wire the `gaps-and-recommendations.mdx` page created in Plan 01 into the Mintlify docs.json navigation so it appears as the final page in the EY Alignment section.

Purpose: GAPS-01 requires readers to receive actionable recommendations. Even if the content exists, readers cannot find it without a navigation entry. This plan adds the page to the existing EY Alignment navigation group in docs.json.

Output: Updated `docs/docs.json` with `ey-framework-alignment/gaps-and-recommendations` appended to the EY Alignment navigation group.
</objective>

<execution_context>
@/home/khenzo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/khenzo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/docs.json
@.planning/phases/16-ey-framework-alignment-overview/16-02-PLAN.md
</context>

<interfaces>
<!-- Current docs.json structure (from Phase 16-02 PLAN): -->
<!--
  The EY Alignment tab and navigation group were added in Phase 16-02.
  Expected current navigation group for EY Alignment:
  {
    "group": "EY Framework Alignment",
    "tab": "EY Alignment",
    "pages": [
      "ey-framework-alignment/index",
      "ey-framework-alignment/alignment-map"
    ]
  }
-->

<!-- New page to add (from Plan 01): -->
<!--
  - ey-framework-alignment/gaps-and-recommendations
-->

<!-- Mintlify convention: -->
<!--
  - The "tab" field in a navigation group MUST match the "name" field of a tab in the tabs array exactly.
  - Pages are referenced without file extension.
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append gaps-and-recommendations to EY Alignment navigation group</name>
  <files>docs/docs.json</files>
  <action>
Read `docs/docs.json` fully. Locate the navigation group for EY Framework Alignment (the group with `"tab": "EY Alignment"`). Append `"ey-framework-alignment/gaps-and-recommendations"` to the end of its `pages` array.

If the EY Alignment group does not yet exist (e.g., Phase 16 has not been executed), create it at the end of the `navigation.groups` array with:
```json
{
  "group": "EY Framework Alignment",
  "tab": "EY Alignment",
  "pages": [
    "ey-framework-alignment/index",
    "ey-framework-alignment/alignment-map",
    "ey-framework-alignment/gaps-and-recommendations"
  ]
}
```

Ensure valid JSON formatting (proper commas, no trailing commas).
  </action>
  <verify>
`docs/docs.json` contains `ey-framework-alignment/gaps-and-recommendations` inside the EY Alignment navigation group's pages array.
  </verify>
  <done>Navigation entry added successfully</done>
</task>

<task type="auto">
  <name>Task 2: Validate JSON and Mintlify build</name>
  <files>docs/docs.json</files>
  <action>
Run a JSON validation check on `docs/docs.json` (e.g., `node -e "JSON.parse(require('fs').readFileSync('docs/docs.json'))"`). Then run the Mintlify build or development command to verify the navigation change does not break the site.
  </action>
  <verify>JSON parses without errors and Mintlify build succeeds</verify>
  <done>docs.json is valid and site builds cleanly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docs/docs.json` contains `ey-framework-alignment/gaps-and-recommendations` in the EY Alignment group's pages
- [ ] `docs/docs.json` is valid JSON (no parse errors)
- [ ] Mintlify build or dev server starts without navigation errors
- [ ] The EY Alignment tab still resolves correctly
</verification>

<success_criteria>
- Navigation update is clean and minimal
- JSON remains valid
- Build passes
- New page is reachable from the EY Alignment sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/20-attention-points-recommendations/20-02-SUMMARY.md`
</output>
