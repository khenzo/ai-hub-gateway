---
phase: 04-implementation-guides
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/mint.json
autonomous: true
gap_closure: true
requirements:
  - REQ-04
  - REQ-05

must_haves:
  truths:
    - "All 10 Phase 4 guide files are reachable from the Mintlify navigation"
    - "Getting Started group contains quick-start and roles-and-responsibilities pages"
    - "A Guides navigation group exists covering all 8 guides/* files"
    - "No nav entry references a path that does not exist on disk"
  artifacts:
    - path: "docs/mint.json"
      provides: "Mintlify navigation config with all Phase 4 files registered"
      contains: "getting-started/quick-start"
    - path: "docs/mint.json"
      provides: "Guides group"
      contains: "guides/deployment-guide"
  key_links:
    - from: "docs/mint.json navigation"
      to: "docs/getting-started/quick-start.mdx"
      via: "getting-started/quick-start page entry"
      pattern: "getting-started/quick-start"
    - from: "docs/mint.json navigation"
      to: "docs/guides/deployment-guide.mdx"
      via: "guides/deployment-guide page entry"
      pattern: "guides/deployment-guide"
---

<objective>
Register all 10 orphaned Phase 4 documentation files in the Mintlify navigation config (docs/mint.json).

Purpose: All content was verified substantive and complete (297–983 lines each) but none of the Phase 4 files under getting-started/ or guides/ are reachable by a user visiting the documentation site. The mint.json was initialized in Phase 1 with a skeleton using deployment/ and operations/ path prefixes that do not match where files were actually created.

Output: Updated docs/mint.json with two changes: (1) two files added to the Getting Started group, (2) a new Guides group added covering 8 guides/* files, (3) stale deployment/ and operations/ nav entries that reference non-existent files replaced or removed.
</objective>

<execution_context>
@/home/khenzo/.claude/get-shit-done/workflows/execute-plan.md
@/home/khenzo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-implementation-guides/04-VERIFICATION.md

<interfaces>
<!-- Current docs/mint.json navigation structure (read 2026-04-10) -->

Current navigation groups in docs/mint.json:
1. Getting Started — pages: introduction, what-is-ai-landing-zone, citadel-platform-overview, business-value, prerequisites
2. Architecture — pages: overview, four-layer-model, hub-spoke-topology, apim-ai-gateway, citadel-governance-hub, agent-spokes, networking, security-fabric
3. Deployment — pages: overview, quick-start, full-deployment, network-configuration, parameter-reference, troubleshooting
   NOTE: deployment/quick-start, deployment/full-deployment, deployment/network-configuration, deployment/parameter-reference, deployment/troubleshooting do NOT exist on disk.
4. Governance — pages: overview, access-contracts, policy-enforcement, identity-management, content-safety, pii-protection
5. Operations — pages: overview, monitoring, observability, cost-management, validation, maintenance
   NOTE: All operations/* pages do NOT exist on disk.

Files that exist on disk and are NOT in nav (the 10 orphans):
- getting-started/quick-start
- getting-started/roles-and-responsibilities
- guides/deployment-guide
- guides/bicep-modules
- guides/deployment-checklist
- guides/validation-checklist
- guides/configuration-examples
- guides/troubleshooting
- guides/common-issues
- guides/best-practices
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mint.json navigation — Getting Started and Guides groups</name>
  <files>docs/mint.json</files>
  <action>
Edit docs/mint.json to make all 10 orphaned Phase 4 files discoverable.

Exact changes required:

1. Getting Started group — append two entries after "getting-started/prerequisites":
   - "getting-started/quick-start"
   - "getting-started/roles-and-responsibilities"

2. Replace the Deployment group entirely. The current Deployment group references 5 paths that do not exist on disk (deployment/quick-start, deployment/full-deployment, deployment/network-configuration, deployment/parameter-reference, deployment/troubleshooting). Replace this group with a Guides group using the actual file paths:
   {
     "group": "Guides",
     "pages": [
       "guides/deployment-guide",
       "guides/bicep-modules",
       "guides/deployment-checklist",
       "guides/validation-checklist",
       "guides/configuration-examples",
       "guides/troubleshooting",
       "guides/common-issues",
       "guides/best-practices"
     ]
   }

3. Remove the Operations group entirely (all 6 operations/* paths do not exist on disk and will cause Mintlify nav errors). If a tab references "operations" URL, remove that tab entry too.

4. Also update the tabs array: change the "Deployment" tab entry (url: "deployment") to reference "guides" instead, so users can reach the new Guides group from the top navigation:
   { "name": "Guides", "url": "guides" }

   Remove the "Operations" tab entry (url: "operations") since the group no longer exists.

The resulting navigation.groups array should have: Getting Started, Architecture, Guides, Governance.
The resulting tabs array should have: Getting Started, Architecture, Guides, Governance.

Preserve all other fields (name, logo, favicon, colors, topbarLinks, topbarCtaButton, anchors, footerSocials) exactly as-is.
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const nav = JSON.parse(fs.readFileSync('docs/mint.json', 'utf8'));
const groups = nav.navigation.groups;
const gsGroup = groups.find(g => g.group === 'Getting Started');
const guidesGroup = groups.find(g => g.group === 'Guides');

const checks = [
  ['Getting Started group exists', !!gsGroup],
  ['quick-start in Getting Started', gsGroup && gsGroup.pages.includes('getting-started/quick-start')],
  ['roles-and-responsibilities in Getting Started', gsGroup && gsGroup.pages.includes('getting-started/roles-and-responsibilities')],
  ['Guides group exists', !!guidesGroup],
  ['deployment-guide in Guides', guidesGroup && guidesGroup.pages.includes('guides/deployment-guide')],
  ['bicep-modules in Guides', guidesGroup && guidesGroup.pages.includes('guides/bicep-modules')],
  ['deployment-checklist in Guides', guidesGroup && guidesGroup.pages.includes('guides/deployment-checklist')],
  ['validation-checklist in Guides', guidesGroup && guidesGroup.pages.includes('guides/validation-checklist')],
  ['configuration-examples in Guides', guidesGroup && guidesGroup.pages.includes('guides/configuration-examples')],
  ['troubleshooting in Guides', guidesGroup && guidesGroup.pages.includes('guides/troubleshooting')],
  ['common-issues in Guides', guidesGroup && guidesGroup.pages.includes('guides/common-issues')],
  ['best-practices in Guides', guidesGroup && guidesGroup.pages.includes('guides/best-practices')],
  ['no stale deployment/quick-start', !JSON.stringify(nav).includes('\"deployment/quick-start\"')],
  ['no stale deployment/full-deployment', !JSON.stringify(nav).includes('\"deployment/full-deployment\"')],
  ['no orphaned operations group', !groups.find(g => g.group === 'Operations')],
];

let passed = 0;
checks.forEach(([name, result]) => {
  console.log((result ? 'PASS' : 'FAIL') + ': ' + name);
  if (result) passed++;
});
console.log(passed + '/' + checks.length + ' checks passed');
process.exit(passed === checks.length ? 0 : 1);
"
    </automated>
  </verify>
  <done>
All 10 orphaned Phase 4 files are registered in docs/mint.json. Getting Started group has 7 pages (original 5 + quick-start + roles-and-responsibilities). A Guides group exists with all 8 guides/* pages. The Deployment group (with its 5 non-existent path references) and Operations group (with its 6 non-existent path references) are replaced/removed. Node validation script exits 0 with 14/14 checks passed.
  </done>
</task>

</tasks>

<verification>
Run the automated node script from Task 1 to confirm all 10 files are registered and no stale nav entries remain:

```bash
node -e "
const fs = require('fs');
const nav = JSON.parse(fs.readFileSync('docs/mint.json', 'utf8'));
const allPages = nav.navigation.groups.flatMap(g => g.pages);
const required = [
  'getting-started/quick-start',
  'getting-started/roles-and-responsibilities',
  'guides/deployment-guide',
  'guides/bicep-modules',
  'guides/deployment-checklist',
  'guides/validation-checklist',
  'guides/configuration-examples',
  'guides/troubleshooting',
  'guides/common-issues',
  'guides/best-practices'
];
const missing = required.filter(p => !allPages.includes(p));
console.log(missing.length === 0 ? 'All 10 Phase 4 files in nav' : 'Missing: ' + missing.join(', '));
"
```

Expected: `All 10 Phase 4 files in nav`

Additionally, manually run `mintlify dev` from the docs/ directory and confirm: Getting Started, Architecture, Guides, and Governance tabs appear; all 10 Phase 4 pages are navigable and render without errors.
</verification>

<success_criteria>
- docs/mint.json is valid JSON (node can parse it without error)
- Getting Started group includes getting-started/quick-start and getting-started/roles-and-responsibilities
- A Guides group exists containing all 8 guides/* files
- No nav entry references a path that does not exist on disk (deployment/quick-start, deployment/full-deployment, operations/*, etc. are removed)
- Node validation script: 14/14 checks passed, exit code 0
- All three verification gaps from 04-VERIFICATION.md are closed (REQ-04 Getting Started wiring, REQ-04 Deployment guide wiring, REQ-05 Configuration/best-practices wiring)
</success_criteria>

<output>
After completion, create `.planning/phases/04-implementation-guides/04-04-SUMMARY.md` summarizing:
- What was changed in docs/mint.json (groups added, stale entries removed)
- Verification script result (N/14 checks)
- Confirmation that all 10 orphaned Phase 4 files are now nav-wired
</output>
