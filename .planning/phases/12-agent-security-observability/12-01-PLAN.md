---
phase: 12-agent-security-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/agent-factory/agent-security-blueprint.mdx
  - docs.json
autonomous: true
requirements:
  - 12-01

must_haves:
  truths:
    - User can read a comprehensive agent security blueprint page
    - The security blueprint page appears in the Agent Factory navigation group before the use-case pages
    - Content covers the 5 qualities of safe agents mapped to Citadel layers
    - Entra Agent ID is documented with a preview/coming-soon caveat
    - Cross-prompt injection classifier and Prompt Shields are documented
    - Page links back to existing governance docs (no duplication)
    - CardGroup navigation connects to adjacent pages
  artifacts:
    - path: "docs/agent-factory/agent-security-blueprint.mdx"
      provides: "Agent security blueprint with Citadel layer mapping, 6-step blueprint, and cross-references"
      min_lines: 80
    - path: "docs.json"
      provides: "Navigation wired: agent-security-blueprint inserted after a2a-integration, before use-case pages"
      contains: "agent-security-blueprint"
  key_links:
    - from: "docs.json"
      to: "docs/agent-factory/agent-security-blueprint.mdx"
      via: "Mintlify navigation"
      pattern: "agent-security-blueprint"
    - from: "docs/agent-factory/agent-security-blueprint.mdx"
      to: "docs/governance/identity-management.mdx"
      via: "href cross-reference"
      pattern: "identity-management"
---

<objective>
Document the Agent Factory security blueprint — five qualities of safe agents, the 6-step blueprint in action, and Citadel layer-by-layer mapping — as a new page in the Agent Factory section.

Purpose: Provide security architects and platform engineers with actionable guidance for building safe, secure, and trustworthy agents on Citadel, complementing the existing governance section with agent-specific security controls.
Output: `docs/agent-factory/agent-security-blueprint.mdx` and updated `docs.json` navigation.
</objective>

<execution_context>
@/home/khenzo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/khenzo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Source Material
@docs/agent-factory/creating-a-blueprint-for-safe-and-secure-ai-agents.md

## Existing Pages — Do Not Duplicate (cross-link instead)
@docs/governance/identity-management.mdx
@docs/governance/security-compliance.mdx
@docs/architecture/layer-4-security-fabric.mdx
@docs/governance/content-safety.mdx

## Reference Pattern (page structure and MDX conventions)
@docs/agent-factory/a2a-integration.mdx

## Navigation File
@docs.json

<interfaces>
<!-- MDX frontmatter pattern used in all agent-factory pages -->
---
title: "Page Title"
description: "One-line description"
---

<!-- CardGroup footer pattern (mandatory on all agent-factory pages) -->
<CardGroup cols={2}>
  <Card title="..." icon="..." href="/agent-factory/...">...</Card>
  <Card title="..." icon="..." href="/governance/...">...</Card>
</CardGroup>

<!-- docs.json Agent Factory group — current order (lines 103-118) -->
"docs/agent-factory/a2a-integration",
"docs/agent-factory/use-case-employee-self-service",
<!-- INSERT agent-security-blueprint BETWEEN these two entries -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent security blueprint documentation</name>
  <files>docs/agent-factory/agent-security-blueprint.mdx</files>
  <action>
    Create `docs/agent-factory/agent-security-blueprint.mdx` using the source doc at
    `docs/agent-factory/creating-a-blueprint-for-safe-and-secure-ai-agents.md` as the canonical reference.

    Page structure (in order):

    1. **Frontmatter**
       ```
       title: "Agent Security Blueprint"
       description: "A layered approach to building safe, secure, and trustworthy AI agents on Citadel"
       ```

    2. **Intro paragraph** — Link agent trust to Citadel governance. State that this page covers agent-specific
       controls that extend the existing governance layer (cross-link to security-compliance.mdx and
       identity-management.mdx with a `<Note>` directing readers there for the enterprise-level baseline).

    3. **"Why Now: The Security Imperative" section** — Short paragraph on the CISO/developer shift-left
       moment. Three bullet points on key threat vectors agents introduce (prompt injection, data exfiltration,
       shadow agents). Do NOT re-document general AI security already in security-compliance.mdx.

    4. **"Five Qualities of Safe Agents" section** — Render as a Mintlify table or short numbered list:
       - Identity: unique traceable identity per agent
       - Data protection: sensitivity labels, DLP, bring-your-own resources
       - Built-in controls: Prompt Shields, harm/risk filters, groundedness checks, protected material detection
       - Evaluations: automated red teaming and quality gates
       - Continuous oversight: runtime governance and Defender XDR telemetry

    5. **"Citadel Implementation" section** — Five sub-sections, one per quality:
       - **Identity:** Entra Agent ID (wrap in `<Note>` flagging preview/coming-soon status per Sep 2025 source).
         Cross-reference identity-management.mdx for existing 3 identity types. Do NOT duplicate that content.
       - **Data Protection:** Bring-your-own Azure resources (file storage, search, conversation history),
         network isolation with private VNet and subnet delegation, Microsoft Purview sensitivity labels + DLP.
         Cross-reference layer-4-security-fabric.mdx for network isolation patterns.
       - **Built-in Controls:** Cross-prompt injection classifier (scans tool responses, email triggers, and
         untrusted sources — not just user input), Prompt Shields, harm/risk filters, groundedness checks,
         protected material detection. Cross-reference content-safety.mdx for existing filter configuration.
       - **Evaluations:** Azure AI Red Teaming Agent + PyRIT toolkit for adversarial testing at scale.
         Note: validates multi-agent workflows before production.
       - **Continuous Oversight:** Microsoft Defender XDR telemetry integration, Governance collaborators
         Credo AI and Saidot (EU AI Act and NIST AI RMF mapping).

    6. **"Blueprint in Action: 6-Step Sequence" section** — Use `<Steps>` component:
       Step 1: Assign Entra Agent ID at creation
       Step 2: Apply content filters and cross-prompt injection classifier
       Step 3: Configure data protection (Purview labels, BYOR, VNet isolation)
       Step 4: Run evaluations and red team before promotion to production
       Step 5: Deploy with Defender XDR telemetry enabled
       Step 6: Continuous governance review with Credo AI / Saidot

    7. **"Citadel Layer Mapping" ASCII diagram** — Map each blueprint element to its Citadel layer:
       ```
       ┌─────────────────────────────────────────────────────────────────────────────┐
       │                    SECURITY BLUEPRINT ON CITADEL                            │
       │                                                                             │
       │  Blueprint Element              Citadel Layer                               │
       │  ─────────────────────────────────────────────                             │
       │  Entra Agent ID                 Layer 3: Agent Identity                     │
       │  Cross-Prompt Injection         Layer 4: Security Fabric                    │
       │  Prompt Shields / Filters       Layer 4: Security Fabric                    │
       │  Data Protection (Purview)      Layer 1: Governance Hub                     │
       │  Network Isolation (VNet)       Layer 4: Security Fabric                    │
       │  Evaluations / Red Teaming      Layer 2: AI Control Plane                  │
       │  Defender XDR Integration       Layer 4: Security Fabric                    │
       │  Governance (Credo AI/Saidot)   Layer 1: Governance Hub                     │
       └─────────────────────────────────────────────────────────────────────────────┘
       ```

    8. **"Enterprise Proof Points" section** — 2-3 brief customer context paragraphs (EY, Accenture) sourced
       from the source doc. Keep concise — one sentence each with the relevant insight.

    9. **CardGroup footer** (mandatory) — 4 cards:
       - Agent Observability → `/agent-factory/agent-observability` (icon: chart-line)
       - Identity Management → `/governance/identity-management` (icon: id-card)
       - Security Fabric → `/architecture/layer-4-security-fabric` (icon: shield)
       - Content Safety → `/governance/content-safety` (icon: filter)

    Style rules:
    - Match tone of a2a-integration.mdx (technical but accessible)
    - Use `<Note>` for Entra Agent ID preview caveat and any feature not yet GA
    - Use `<Warning>` for cross-prompt injection to emphasise the broader attack surface vs user-input-only
    - Do NOT duplicate content from security-compliance.mdx, identity-management.mdx, content-safety.mdx,
      or layer-4-security-fabric.mdx — cross-link instead
    - Target 100-150 lines of MDX content
  </action>
  <verify>
    <automated>ls docs/agent-factory/agent-security-blueprint.mdx && wc -l docs/agent-factory/agent-security-blueprint.mdx | awk '$1 >= 80 {print "LINE_COUNT_OK"}'</automated>
  </verify>
  <done>File exists with valid MDX frontmatter, at least 80 lines, contains all five qualities, 6-step Steps component, ASCII diagram, and CardGroup footer</done>
</task>

<task type="auto">
  <name>Task 2: Wire security blueprint into docs.json navigation</name>
  <files>docs.json</files>
  <action>
    Edit `docs.json` to add `docs/agent-factory/agent-security-blueprint` to the Agent Factory navigation group.

    Current order (excerpt):
    ```
    "docs/agent-factory/a2a-integration",
    "docs/agent-factory/use-case-employee-self-service",
    ```

    Insert the new entry BETWEEN these two lines:
    ```
    "docs/agent-factory/a2a-integration",
    "docs/agent-factory/agent-security-blueprint",
    "docs/agent-factory/use-case-employee-self-service",
    ```

    Maintain valid JSON (correct commas, no trailing comma on last item in array). Do not change any other
    navigation groups. After editing, verify the file parses as valid JSON.
  </action>
  <verify>
    <automated>grep "agent-security-blueprint" docs.json && node -e "require('./docs.json')" && echo "JSON_VALID"</automated>
  </verify>
  <done>docs.json is valid JSON and contains agent-security-blueprint in the Agent Factory group after a2a-integration and before use-case-employee-self-service</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docs/agent-factory/agent-security-blueprint.mdx` exists with >= 80 lines
- [ ] File has valid MDX frontmatter (title, description)
- [ ] Five qualities section present with Citadel sub-sections
- [ ] 6-step `<Steps>` component present
- [ ] ASCII Citadel layer mapping diagram present
- [ ] Entra Agent ID section has `<Note>` with preview/coming-soon caveat
- [ ] Cross-prompt injection section uses `<Warning>` component
- [ ] CardGroup footer present with 4 cards linking to valid existing pages
- [ ] Cross-references to security-compliance.mdx, identity-management.mdx, content-safety.mdx, layer-4-security-fabric.mdx — no duplication of those pages' content
- [ ] docs.json is valid JSON
- [ ] agent-security-blueprint appears in Agent Factory group after a2a-integration and before use-case-employee-self-service
</verification>

<success_criteria>
- Agent security blueprint document created with comprehensive coverage of 5 qualities, 6-step sequence, and Citadel mapping
- Entra Agent ID preview caveat present
- No content duplication with existing governance docs — cross-links used throughout
- Navigation correctly ordered: conceptual pages before use-case pages
- Page renders in Mintlify with all components (Steps, Note, Warning, CardGroup, ASCII diagram)
</success_criteria>

<output>
After completion, create `.planning/phases/12-agent-security-observability/12-01-SUMMARY.md`
</output>
