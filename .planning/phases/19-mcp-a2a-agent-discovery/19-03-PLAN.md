---
phase: 19-mcp-a2a-agent-discovery
plan: 03
type: execute
wave: 2
depends_on:
  - 19-01
  - 19-02
files_modified:
  - docs/docs.json
autonomous: true
requirements:
  - PROTO-01
  - PROTO-02
  - PROTO-03

must_haves:
  truths:
    - "All three Phase 19 pages are reachable from the EY Alignment section sidebar navigation"
    - "The EY Alignment tab is already present in the nav (added in Phase 16) — Phase 19 extends the existing navigation group"
  artifacts:
    - path: "docs/docs.json"
      provides: "Updated navigation group for EY Alignment tab — adds mcp-governance, a2a-trust-broker, and agent-discovery pages to the existing EY Alignment navigation group"
      contains: "ey-framework-alignment/mcp-governance"
  key_links:
    - from: "docs/docs.json navigation groups EY Framework Alignment"
      to: "docs/ey-framework-alignment/mcp-governance.mdx"
      via: "pages array entry"
      pattern: "ey-framework-alignment/mcp-governance"
    - from: "docs/docs.json navigation groups EY Framework Alignment"
      to: "docs/ey-framework-alignment/a2a-trust-broker.mdx"
      via: "pages array entry"
      pattern: "ey-framework-alignment/a2a-trust-broker"
    - from: "docs/docs.json navigation groups EY Framework Alignment"
      to: "docs/ey-framework-alignment/agent-discovery.mdx"
      via: "pages array entry"
      pattern: "ey-framework-alignment/agent-discovery"
---

<objective>
Wire the three Phase 19 pages into the existing EY Alignment navigation group in docs.json.

Purpose: Pages created in Plans 01 and 02 are invisible in the Mintlify site until listed in docs.json. Phase 16 already added the "EY Alignment" tab and a navigation group. This plan extends that group with the three new pages under a "Protocols & Discovery" sub-group.

Output: Updated `docs/docs.json` with three new page entries in the EY Alignment navigation.
</objective>

<execution_context>
@/home/khenzo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/khenzo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/docs.json
</context>

<interfaces>
<!-- Expected EY Alignment navigation group (added by Phase 16-02-PLAN, extended by 17-02 and 18-03): -->
<!--
  By the time Phase 19 executes, the EY Framework Alignment group should look something like:
  {
    "group": "EY Framework Alignment",
    "tab": "EY Alignment",
    "pages": [
      "ey-framework-alignment/index",
      "ey-framework-alignment/alignment-map",
      {
        "group": "Kubernetes & DevOps",
        "icon": "server",
        "pages": [ ... ]
      },
      {
        "group": "Security & Governance",
        "icon": "shield",
        "pages": [
          "ey-framework-alignment/governance-tiers",
          "ey-framework-alignment/guardrails-config",
          "ey-framework-alignment/compliance-tooling"
        ]
      }
    ]
  }

  Phase 19 must EXTEND this group by adding a nested sub-group for Protocols & Discovery.
  Do NOT replace or remove the existing pages or sub-groups.
-->

<!-- Mintlify nested group pattern: -->
<!--
  Nested groups are objects within the pages array:
  {
    "group": "Protocols & Discovery",
    "icon": "network",
    "pages": [
      "ey-framework-alignment/mcp-governance",
      "ey-framework-alignment/a2a-trust-broker",
      "ey-framework-alignment/agent-discovery"
    ]
  }
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend EY Alignment navigation group with Protocols & Discovery sub-group</name>
  <files>docs/docs.json</files>
  <action>
Read `docs/docs.json` fully. Locate the navigation group with `"group": "EY Framework Alignment"` and `"tab": "EY Alignment"`.

**Expected current state of that group (after Phases 16, 17, 18):**
```json
{
  "group": "EY Framework Alignment",
  "tab": "EY Alignment",
  "pages": [
    "ey-framework-alignment/index",
    "ey-framework-alignment/alignment-map",
    {
      "group": "Kubernetes & DevOps",
      "icon": "server",
      "pages": [
        "ey-framework-alignment/aks-deployment",
        "ey-framework-alignment/github-scaffolding",
        "ey-framework-alignment/cicd-pipeline"
      ]
    },
    {
      "group": "Security & Governance",
      "icon": "shield",
      "pages": [
        "ey-framework-alignment/governance-tiers",
        "ey-framework-alignment/guardrails-config",
        "ey-framework-alignment/compliance-tooling"
      ]
    }
  ]
}
```

**Required change:** Append a nested sub-group object to the `pages` array of that group. The result must include:
```json
{
  "group": "Protocols & Discovery",
  "icon": "network",
  "pages": [
    "ey-framework-alignment/mcp-governance",
    "ey-framework-alignment/a2a-trust-broker",
    "ey-framework-alignment/agent-discovery"
  ]
}
```

**Rules:**
- Do NOT alter any other navigation group, tab, or page entry in docs.json
- Preserve all existing formatting (2-space indentation, trailing commas where present)
- The file must remain valid JSON after the edit
- The "EY Framework Alignment" group's existing pages and prior sub-groups (Kubernetes & DevOps, Security & Governance) must remain intact

**Defensive handling:** If the EY Framework Alignment group does not yet exist (e.g., Phase 16 has not executed), create it at the end of the navigation.groups array with:
```json
{
  "group": "EY Framework Alignment",
  "tab": "EY Alignment",
  "pages": [
    "ey-framework-alignment/index",
    "ey-framework-alignment/alignment-map",
    {
      "group": "Protocols & Discovery",
      "icon": "network",
      "pages": [
        "ey-framework-alignment/mcp-governance",
        "ey-framework-alignment/a2a-trust-broker",
        "ey-framework-alignment/agent-discovery"
      ]
    }
  ]
}
```
And add the "EY Alignment" tab to the tabs array if it is missing:
```json
{
  "name": "EY Alignment",
  "url": "ey-framework-alignment"
}
```
  </action>
  <verify>
    <automated>python3 -c "
import json
path = '/home/khenzo/projects/ai-hub-gateway/docs/docs.json'
d = json.load(open(path))
groups = d['navigation']['groups']
ey_group = next((g for g in groups if g.get('group') == 'EY Framework Alignment'), None)
assert ey_group is not None, 'EY Framework Alignment group missing'
pages = ey_group['pages']
assert any('ey-framework-alignment/index' == p for p in pages if isinstance(p, str)), 'index page missing'
nested = next((p for p in pages if isinstance(p, dict) and p.get('group') == 'Protocols & Discovery'), None)
assert nested is not None, 'Protocols & Discovery sub-group missing'
sub_pages = nested['pages']
assert 'ey-framework-alignment/mcp-governance' in sub_pages, 'mcp-governance missing'
assert 'ey-framework-alignment/a2a-trust-broker' in sub_pages, 'a2a-trust-broker missing'
assert 'ey-framework-alignment/agent-discovery' in sub_pages, 'agent-discovery missing'
tabs = d.get('tabs', [])
assert any(t.get('name') == 'EY Alignment' for t in tabs), 'EY Alignment tab missing'
print('VALID')
"</automated>
  </verify>
  <done>docs.json is valid JSON; EY Framework Alignment group exists with index/alignment-map intact; new Protocols & Discovery sub-group present with all three Phase 19 pages; EY Alignment tab present</done>
</task>

</tasks>

<verification>
- `python3 -c "import json; json.load(open('docs/docs.json'))"` exits cleanly
- EY Framework Alignment group contains index, alignment-map, and any prior sub-groups (Kubernetes & DevOps, Security & Governance)
- New Protocols & Discovery sub-group contains: mcp-governance, a2a-trust-broker, agent-discovery
- No other groups, tabs, or pages in docs.json changed
- Total Phase 19 pages are now navigable from the EY Alignment tab sidebar
</verification>

<success_criteria>
- All three Phase 19 content pages are reachable from the EY Alignment tab sidebar navigation
- docs.json remains valid JSON with no regressions to existing navigation
- Phase 16 pages (index, alignment-map) and prior phase sub-groups remain intact
</success_criteria>

<output>
After completion, create `.planning/phases/19-mcp-a2a-agent-discovery/19-03-SUMMARY.md`
</output>
