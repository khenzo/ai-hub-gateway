        <!-- Capacity Management - Subscription Level Token Limits -->
        <llm-token-limit counter-key="@(context.Subscription.Id)" tokens-per-minute="{{TPM}}" estimate-prompt-tokens="false" tokens-consumed-header-name="consumed-tokens" remaining-tokens-header-name="remaining-tokens" token-quota="{{QUOTA}}" token-quota-period="{{QUOTA_PERIOD}}" retry-after-header-name="retry-after" />