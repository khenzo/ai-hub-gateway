:---
title: 'API Endpoint Name'
description: 'Documentation for [endpoint] API endpoint'
icon: 'api'
---

# [Endpoint Name]

{/* One-sentence description */}

[Brief description of what this endpoint does and its primary purpose].

<Info>
  **Base URL**: `https://api.example.com/v1`  
  **Endpoint**: `METHOD /path/to/endpoint`  
  **Authentication**: [Required/Optional]
</Info>

## Overview

{/* High-level summary */}

| Attribute | Value |
|-----------|-------|
| **Method** | GET/POST/PUT/DELETE |
| **Path** | `/v1/resource/{id}` |
| **Rate Limit** | 1000 requests/hour |
| **Timeout** | 30 seconds |

## Authentication

{/* Authentication requirements */}

This endpoint requires [authentication method] via the `Authorization` header.

```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  https://api.example.com/v1/endpoint
```

### Required Headers

| Header | Value | Required |
|--------|-------|----------|
| `Authorization` | `Bearer YOUR_TOKEN` | Yes |
| `Content-Type` | `application/json` | For POST/PUT |
| `X-Request-ID` | Unique request ID | Recommended |

## Request

### Path Parameters

<ParamField path="id" type="string" required>
  Unique identifier for the resource
</ParamField>

### Query Parameters

<ParamField query="include" type="string" optional>
  Comma-separated list of related resources to include
  <br />
  **Example**: `include=metadata,permissions`
</ParamField>

<ParamField query="limit" type="integer" default="20">
  Maximum number of results to return (1-100)
</ParamField>

<ParamField query="offset" type="integer" default="0">
  Number of results to skip for pagination
</ParamField>

### Request Body

{/* For POST/PUT endpoints */}

<ParamField body="name" type="string" required>
  Resource name (1-100 characters)
</ParamField>

<ParamField body="config" type="object" required>
  Configuration object
  <Expandable title="Properties">
    <ParamField body="config.setting1" type="string" required>
      Description of setting1
    </ParamField>
    <ParamField body="config.setting2" type="boolean" default="false">
      Description of setting2
    </ParamField>
  </Expandable>
</ParamField>

### Request Example

```bash
# GET request with query parameters
curl -X GET "https://api.example.com/v1/resource/123?include=metadata&limit=10" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

```bash
# POST request with body
curl -X POST "https://api.example.com/v1/resource" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "My Resource",
    "config": {
      "setting1": "value1",
      "setting2": true
    }
  }'
```

## Response

### Success Response

**Status**: `200 OK` (or 201 Created for POST)

<ResponseField name="id" type="string" required>
  Unique identifier for the resource
</ResponseField>

<ResponseField name="name" type="string" required>
  Resource name
</ResponseField>

<ResponseField name="status" type="string" required>
  Current status of the resource
</ResponseField>

<ResponseField name="created_at" type="string" required>
  ISO 8601 timestamp when the resource was created
</ResponseField>

<ResponseField name="metadata" type="object" optional>
  Additional metadata
  <Expandable title="Properties">
    <ResponseField name="metadata.version" type="string">
      Resource version
    </ResponseField>
    <ResponseField name="metadata.tags" type="array">
      Array of string tags
    </ResponseField>
  </Expandable>
</ResponseField>

### Response Example

```json
{
  "id": "resource-123",
  "name": "My Resource",
  "status": "active",
  "created_at": "2024-01-15T10:30:00Z",
  "metadata": {
    "version": "1.0.0",
    "tags": ["production", "team-a"]
  }
}
```

## Error Responses

### 400 Bad Request

```json
{
  "error": {
    "code": "INVALID_REQUEST",
    "message": "Request validation failed",
    "details": [
      {
        "field": "name",
        "message": "Name is required"
      }
    ]
  }
}
```

### 401 Unauthorized

```json
{
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Invalid or missing authentication token"
  }
}
```

### 404 Not Found

```json
{
  "error": {
    "code": "NOT_FOUND",
    "message": "Resource with ID '123' not found"
  }
}
```

### 429 Rate Limited

```json
{
  "error": {
    "code": "RATE_LIMITED",
    "message": "Too many requests. Retry after 60 seconds."
  }
}
```

## Error Codes

| Status | Code | Description |
|--------|------|-------------|
| 400 | `INVALID_REQUEST` | Request body validation failed |
| 401 | `UNAUTHORIZED` | Authentication required or invalid |
| 403 | `FORBIDDEN` | Insufficient permissions |
| 404 | `NOT_FOUND` | Resource does not exist |
| 409 | `CONFLICT` | Resource already exists or state conflict |
| 422 | `UNPROCESSABLE` | Request valid but cannot be processed |
| 429 | `RATE_LIMITED` | Too many requests |
| 500 | `INTERNAL_ERROR` | Server error (contact support) |

## Rate Limiting

- **Default limit**: 1000 requests per hour
- **Burst limit**: 100 requests per minute
- **Headers**: Every response includes rate limit headers:
  - `X-RateLimit-Limit`: Maximum requests allowed
  - `X-RateLimit-Remaining`: Remaining requests in window
  - `X-RateLimit-Reset`: Unix timestamp when limit resets

<Warning>
  Exceeding rate limits will result in 429 responses. Implement exponential backoff for retries.
</Warning>

## SDK Examples

<Tabs>
  <Tab title="Python">
    ```python
    import requests
    from typing import Dict, Any
    
    class APIClient:
        def __init__(self, api_key: str):
            self.base_url = "https://api.example.com/v1"
            self.headers = {
                "Authorization": f"Bearer {api_key}",
                "Content-Type": "application/json"
            }
        
        def get_resource(self, resource_id: str) -> Dict[str, Any]:
            response = requests.get(
                f"{self.base_url}/resource/{resource_id}",
                headers=self.headers
            )
            response.raise_for_status()
            return response.json()
        
        def create_resource(self, name: str, config: Dict) -> Dict[str, Any]:
            data = {"name": name, "config": config}
            response = requests.post(
                f"{self.base_url}/resource",
                headers=self.headers,
                json=data
            )
            response.raise_for_status()
            return response.json()
    ```
  </Tab>
  <Tab title="JavaScript">
    ```javascript
    class APIClient {
      constructor(apiKey) {
        this.baseUrl = 'https://api.example.com/v1';
        this.headers = {
          'Authorization': `Bearer ${apiKey}`,
          'Content-Type': 'application/json'
        };
      }
      
      async getResource(resourceId) {
        const response = await fetch(
          `${this.baseUrl}/resource/${resourceId}`,
          { headers: this.headers }
        );
        
        if (!response.ok) {
          throw new Error(`HTTP error! status: ${response.status}`);
        }
        
        return await response.json();
      }
      
      async createResource(name, config) {
        const response = await fetch(
          `${this.baseUrl}/resource`,
          {
            method: 'POST',
            headers: this.headers,
            body: JSON.stringify({ name, config })
          }
        );
        
        if (!response.ok) {
          throw new Error(`HTTP error! status: ${response.status}`);
        }
        
        return await response.json();
      }
    }
    ```
  </Tab>
  <Tab title="cURL">
    ```bash
    # Get resource
    curl -X GET "https://api.example.com/v1/resource/123" \
      -H "Authorization: Bearer YOUR_TOKEN"
    
    # Create resource
    curl -X POST "https://api.example.com/v1/resource" \
      -H "Authorization: Bearer YOUR_TOKEN" \
      -H "Content-Type: application/json" \
      -d '{
        "name": "My Resource",
        "config": {"setting1": "value1"}
      }'
    ```
  </Tab>
</Tabs>

## Changelog

| Version | Date | Changes |
|---------|------|---------|
| v1.1 | 2024-02-01 | Added `include` query parameter |
| v1.0 | 2024-01-15 | Initial release |

## Related Endpoints

<CardGroup cols={2}>
  <Card title="List Resources" icon="list" href="/api/resources/list">
    Retrieve a list of all resources
  </Card>
  <Card title="Update Resource" icon="edit" href="/api/resources/update">
    Modify an existing resource
  </Card>
  <Card title="Delete Resource" icon="trash" href="/api/resources/delete">
    Remove a resource
  </Card>
  <Card title="Resource Events" icon="activity" href="/api/resources/events">
    Subscribe to resource events
  </Card>
</CardGroup>

---

{/* 
API DOC TEMPLATE NOTES:
- Update all placeholders with actual endpoint details
- Include all possible parameters with proper types
- Provide request/response examples for all scenarios
- Document all error codes that can be returned
- Include SDK examples in multiple languages
- Keep parameter descriptions concise but complete
- Use ParamField and ResponseField components
- Link to related endpoints
*/}